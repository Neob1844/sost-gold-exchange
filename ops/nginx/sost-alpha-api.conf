# Add to existing nginx server block for sostcore.com
# Serves positions_live.json and other API files with correct CORS headers

location /api/ {
    alias /opt/sost/website/api/;
    add_header Access-Control-Allow-Origin *;
    add_header Cache-Control "no-cache, no-store, must-revalidate";
    expires -1;
}
